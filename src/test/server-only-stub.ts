// Empty stub: `server-only` is a build-time virtual provided by Next.js (it has
// no real node_modules entry). Vitest aliases the import here so server modules
// that guard with `import "server-only"` can be unit-tested under Node.
export {};
